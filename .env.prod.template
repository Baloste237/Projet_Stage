# =========================================================================
# Production Configuration Template
# =========================================================================
# Instructions:
# 1. Generate strong passwords:
#    Windows: powershell -Command "[Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((1..32 | % {[char](Get-Random -Minimum 33 -Maximum 126)})))"
#    Linux/Mac: openssl rand -base64 32
# 
# 2. Update the values below with production secrets
# 3. Never commit this file - use .env.example for templates
# 4. Restrict file permissions: chmod 600 .env.prod
# =========================================================================

# PostgreSQL Database Configuration
DB_NAME=vuln_scanner_prod
DB_USER=scanner_user
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD_32_CHARS_MIN
DB_PORT=5433

# Backend Application Configuration
BACKEND_PORT=8081

# Environment
ENVIRONMENT=production

# =========================================================================
# To use this configuration:
# docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d
# =========================================================================
