spring.application.name=backend
spring.datasource.url=jdbc:postgresql://localhost:5433/vuln_scanner
spring.datasource.username=postgres
spring.datasource.password=admin

spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=org.postgresql.Driver

# Multipart configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

server.port=8081



# App Configuration
app.frontend.url=${APP_FRONTEND_URL:http://localhost:5173}

# IA Engine
sast.engine.url=${SAST_ENGINE_URL:http://localhost:8000/api/v1/analyze}

# MobSF
mobsf.url=${MOBSF_URL:http://localhost:8008}
mobsf.api.key=${MOBSF_API_KEY:mobsf_fixed_api_key_vulnscan_2024}
mobsf.connect-timeout=30
mobsf.read-timeout=600
mobsf.upload-dir=${UPLOAD_DIR:./uploads}

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=loicfranceb@gmail.com
spring.mail.password=elkpffnkdoehjcou
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ── Springdoc OpenAPI / Swagger UI ──
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.swagger-ui.doc-expansion=list
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.persist-authorization=true
springdoc.default-produces-media-type=application/json
springdoc.default-consumes-media-type=application/json
